# Chouten CLI

A development cli to hot reload modules in Chouten.

### Usage

Run the cli:

```bash
cargo run serve /path/to/module_src_dir
```

And in the App, go into the settings, set the IP of your PC, and press connect.
